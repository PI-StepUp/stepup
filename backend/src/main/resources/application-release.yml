spring:
  config:
    activate:
      on-profile: "release"

  initDb:
    enable: false

  datasource:
    url: jdbc:mariadb://52.78.93.184:3306/stepup?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: vkdlxlaWkd123#
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

logging.level:
  com.pi.stepup: debug
  org.hibernate.SQL: debug
  org.hibernate.type: trace

server:
  port: 8080
  ssl:
    key-store: classpath:ssl/keystore.p12
    key-store-type: PKCS12
    key-store-password: vkdlxlaWkd123#
    enabled: true

security:
  require-ssl: true
